# AGENTS.md

本文件供 Codex 和其他开发代理使用，记录 LiveBoard 的工作约定、运行方式、设计决策和容易重复出现的问题。对项目进行修改前应先阅读本文件；完成具有长期影响的改动后同步更新相关章节。

## 项目目标

LiveBoard 是自托管教学工作台。产品设计应优先服务真实教学任务：整理课程内容、授课、练习反馈、权限分配和课程交流。避免为了展示技术而增加低频功能或装饰性界面。

## 代码结构

- `apps/web`：Next.js App Router 前端。
- `apps/api`：NestJS API 与 Prisma 数据访问。
- `packages/shared`：前后端共享类型、权限和评分纯函数。
- `apps/web/lib/routes.ts`：应用路由唯一来源，组件不要手写内部路径。
- `apps/web/lib/api`：前端 API 调用唯一入口，页面组件不要直接调用 `fetch`。
- API 按 `apps/api/src/modules/<domain>` 划分业务域。

## 必须遵守的开发流程

1. 修改前先定位现有实现、共享类型和相关样式，不凭页面截图猜测数据结构。
2. 保留用户已有改动；不要重置、覆盖或清理不属于当前任务的内容。
3. 文件编辑使用补丁方式，避免整文件机械覆盖造成无关差异。
4. 修改共享类型后先构建 `packages/shared`，开发中的 API watcher 可能暂时读取旧的 `dist` 类型。
5. 完成后至少运行相关项目的 typecheck；功能或跨层修改运行 `pnpm validate`。
6. 涉及 UI 时同时检查常规桌面宽度和窄屏，不以“构建通过”代替视觉验证。
7. 不提交 `.env`、密钥、用户数据、构建产物或依赖目录。
8. `apps/web/next-env.d.ts` 由 Next.js 自动生成且不纳入版本控制；Web typecheck 必须先运行 `next typegen`，不要手动修改或提交该文件。

## 开发与生产服务

开发模式：

```bash
pnpm infra:up
pnpm dev
```

- Web：Next.js dev server，端口 3000，支持热更新。
- API：NestJS watch mode，端口 4000。
- PostgreSQL、Redis、MinIO：Docker 容器。

生产容器：

```bash
docker compose up --build -d
```

正式生产部署只使用 GitHub Release 自解压单文件包。服务器不拉取源码、不运行 pnpm、不构建应用镜像，也不直接下载容器镜像；用户在电脑下载 `liveboard-<version>-linux-amd64.run` 后上传服务器，通过 `sudo sh <包> install` 首次安装、`sudo sh <包> upgrade` 升级。安装后统一使用 `/usr/local/bin/liveboard` 管理状态、日志、启停、旧版本清理和可恢复卸载。脚本必须在迁移前备份 PostgreSQL，并等待 API、Web 与 Nginx 都健康后才报告完成。不得使用 `docker compose down -v` 或清理命名卷更新、卸载服务。MinIO 需要独立备份或卷快照。

生产版本固定存放在 `/opt/liveboard/releases/<version>`，`active` 只指向其中一个已通过健康检查的版本；API/Web 镜像使用 Release 版本标签，不再以 `:local` 作为正式发布标签。`liveboard clean` 只能删除明确归属 LiveBoard 的旧版本目录、版本化应用镜像和可选的旧安装包，默认保留当前及上一个版本，永远不触碰 `.env`、备份或数据卷。`liveboard uninstall` 是可恢复卸载，同样保留配置、备份和所有数据卷；彻底删除业务数据不属于普通卸载。

Nginx 站点配置是跨版本持久状态。安装器只在首次缺失时安装带 `# Managed by LiveBoard` 标记的配置，升级不得用发布包模板覆盖现有 HTTP、IP HTTPS 或域名 HTTPS 配置。

域名 HTTPS 使用发布包离线携带的固定版本 ACME 客户端，不依赖特定 DNS 服务商。助手优先检查 HTTP-01；公网 TCP 80 或 HTTP Host 不可用时自动改用需要独占 TCP 443 的 TLS-ALPN-01，并在续期时复用首次成功的验证方式。TLS-ALPN 操作通过临时安装 HTTP-only Nginx 配置并 reload 释放 443，不停止 Nginx systemd unit；完成或失败后必须恢复 HTTPS 配置。最高管理员通过管理中心调用宿主机受限 Unix Socket 助手；API 容器不得挂载 Docker Socket 或获得 root 权限。证书与状态保存在 `/opt/liveboard/https`，systemd timer 每天检查续期。首次切换必须依次通过验证、证书签发、Nginx 校验和本机 TLS 探测；失败时恢复原 Nginx、证书、`.env` 与 `install.conf`。一键流程只覆盖单域名证书，泛域名所需的 DNS-01 不纳入通用流程。HTTPS 助手的 systemd 沙箱必须精确允许访问 `/run/nginx.pid`、`/var/log/nginx` 和 `/var/lib/nginx`，否则 Ubuntu 的 `nginx -t` 会在语法正确后因 PID、日志或临时目录处于只读文件系统而失败；不得为此放宽整个 `/run` 或 `/var`。面板到助手的整条请求链超时必须长于 ACME 操作上限，Nginx 使用 480 秒、API Socket 使用 420 秒。

生产首次初始化由 `apps/api/src/bootstrap-production.ts` 自动完成，只创建一个随机密码的最高管理员、默认 workspace 和论坛分类。部署脚本必须在最终总结中醒目显示首次凭据，并将其保存到权限为 `600` 的 `/opt/liveboard/initial-admin-credentials.txt`；用户修改密码后应删除该文件。升级不得生成或覆盖管理员密码。`apps/api/prisma/seed.cjs` 仅用于本地演示，生产部署不得调用。

`docker-compose.yml` 中的 Web 明确使用 `NODE_ENV=production`。用户要求“开发版本”时，不得只执行 `docker compose up`；应停止 `web`、`api` 容器，保留基础设施，再运行 `pnpm dev`。

在 Codex 沙箱中，本机端口或数据库连接可能返回 `EPERM`/`P1001`。确认基础设施正常后，应在获准的非沙箱环境启动开发进程，不要误判为项目故障。

## Vercel + Cloudflare R2 部署

LiveBoard 支持双目标部署，自托管能力保持不变：

- 自托管：Docker Compose、MinIO、阿里云 OSS、`.run` Release、Nginx 和 HTTPS 助手继续可用。
- Vercel：`apps/web` 与 `apps/api` 分别部署为两个 Vercel Project；PostgreSQL/Redis 使用托管服务；对象存储固定为 Cloudflare R2。

规则：

- 业务行为统一以 `DEPLOYMENT_TARGET=self_hosted|vercel` 为准，缺失默认
  `self_hosted`；业务模块不得直接读 `VERCEL` 决定行为。
- Vercel 下 StorageService 固定使用 R2：active backend 为 `r2`，
  upload/downloadMode 为 `direct`。经过文件头校验的 PNG、JPEG、GIF、WebP
  内联图片由 API 完成权限校验后 302 到 120 秒 R2 签名 GET，图片字节不经过
  Vercel；PDF 预览由 API 完成权限校验后签发更长的预签名 GET，浏览器直连 R2
  流式按需加载。favicon 与用户头像、Markdown/文本预览及其他 inline 内容始终
  由 API 中转。缺失任一
  R2 环境变量时启动失败并列出变量名。存储管理页只读显示 R2，PUT/test 返回
  409。
- 大文件（独立文件、课堂文件、论坛图片、Banner）由浏览器直传 R2（预签名
  PUT，60 秒有效，签入 Content-Type），不经过 Vercel Function Body（普通
  请求/响应体上限 4.5MB）。前端上传指令是 `form_post | put` 判别联合，
  禁止用 `fields` 是否存在猜测协议。Vercel 下直传失败禁止回退 multipart
  中转。
- 临时上传对象统一放 `pending/` 前缀，配合 R2 Lifecycle 一天后兜底删除；
  confirm 时服务端把对象复制到正式业务 Key。`verifyAndFinalizePendingObject`
  是直传确认的统一入口。清理必须先删除临时/正式对象，成功后才能删除
  `PendingUpload`；创建业务记录与释放 PendingUpload 必须在同一数据库事务。
- Vercel 是 Serverless：禁用进程内 `setInterval` 关键清理（过期上传清理改由
  每日 Cron + 惰性清理 + R2 Lifecycle），Server Status 不采样不写表，
  HTTPS 由 Vercel 项目设置托管（写接口 409）。
- Vercel 备份/回滚的自调用接力端点必须立即响应，并用
  `@vercel/functions` 的 `waitUntil()` 托管本棒执行；不得让上游短超时 abort
  一个仍同步等待的接力请求。请求结束后释放实例内哨兵，跨实例互斥以 DB 与
  Redis 为准。
- Neon Snapshot restore POST 非幂等：调用前必须把 requesting 意图写入
  Redis；网络超时只能通过分支 `restored_from` / `restored_as` 元数据确认，
  禁止自动重发 POST。
- Vercel 备份/回滚必须同时配置 `NEON_API_KEY`、`NEON_PROJECT_ID`、
  `REDIS_URL` 与 `CRON_SECRET`；任务进度 `liveboard:backup:job:*` 和互斥锁
  `liveboard:backup:lock:*` 必须使用不同 Redis key，禁止锁释放误删恢复状态；
  Neon 孤儿 Snapshot 清扫必须保留 Redis 活跃任务引用；旧版 `backup-*` 与
  `pre-restore-*` 分支只做兼容清理，不得再创建。Neon Free 只允许一个手动
  Snapshot：手动备份不得覆盖现有快照；自动备份只能轮换应用自己的上一份
  成功自动 Snapshot；回滚预留一个瞬态分支空位，容量不足不得先创建失败行。
  Vercel 启动新任务前的互斥检查必须同时读取 DB 与 Redis 活跃状态，不能依赖
  Serverless 实例上不存在的持久状态文件。Redis 是 Neon 换库期间唯一不随
  快照倒退的任务状态源，不可用或状态写入失败时必须 fail closed，禁止继续
  回滚。Neon Snapshot 只能从默认根分支创建；默认分支非根时必须 fail closed
  并先迁到新的 Neon 项目，不得继续旧式分支恢复。创建/恢复响应中的全部
  operations 都必须完成后才能推进。Vercel 回滚的维护/只读状态存 Redis：
  发出 Neon restore 前开启，数据库校验、对象回拷与清理全部成功后才关闭；读取失败按
  已开启处理，禁止普通用户在换库窗口继续写入。
- Redis 使用全局共享的 `RedisService`，惰性连接；本地开发/测试允许内存
  fallback，Vercel/生产禁止 fallback（Redis 不可用时登录和 AI 返回 503）。
- AI 上游超时固定 110 秒（Web→API rewrite 上限 120 秒）；PDF 预览流式
  pipe，禁止整块读入内存后发送。
- `apps/api/prisma/migrations` 只保留 `00000000000000_baseline_v1` 与
  `migration_lock.toml`。新环境通过 `prisma migrate deploy` 从零建库；既有
  自托管数据库升级必须先运行 `scripts/legacy-baseline-transition.sh`
  （默认只检查，`--execute` 才写入，bridge 后必须通过真实 schema diff，
  未知历史或 drift 一律 fail closed）。Vercel 构建使用 `db:deploy`，优先用
  `DIRECT_DATABASE_URL` 直连迁移，Runtime 仍使用池化 `DATABASE_URL`。旧 41
  条 migration 可由 Git tag `pre-vercel-baseline` 恢复，不得重写 Git 历史。
- 数据迁移工具：`pnpm --filter @liveboard/api migrate-storage-to-r2`
  （默认 dry-run 只能读取源/R2 元数据，严禁写对象或数据库；execute 逐对象
  复制校验后改 `storageBackend=r2`）和
  `verify-vercel-data-migration`（校验行数、外键、最高管理员、AI 解密、
  R2 对象与唯一 baseline）。详见 `docs/migrate-data-to-vercel-r2.md` 与
  `docs/deploy-vercel-r2.md`。
- 所有 R2 Secret、数据库/Redis 地址、`SESSION_SECRET`、`AI_ENCRYPTION_KEY`
  只能通过环境变量提供，禁止写入代码、日志、文档真实值或 `NEXT_PUBLIC_*`。
- Vercel Web 的 `STATIC_ASSET_PROVIDER=vercel|cloudflare|edgeone` 选择
  `/_next/static/*` 使用 Vercel 原生分发、Cloudflare Pages 或 Tencent EdgeOne
  Makers；默认 `vercel`。Cloudflare 与 EdgeOne 的域名、项目名和 Secret 可以同时
  保留，切换 provider 时 `postbuild` 只向选中的 Direct Upload Project 上传本次
  相同构建，并通过正式静态域名逐字节校验构建文件（含存在时的 `.mjs`）的
  JavaScript Content-Type；PDF.js worker 由 Web 站点从 `public/` 提供，不进入
  EdgeOne（EdgeOne 上传把 `.mjs` 存成 `application/octet-stream`，而 `new URL()`
  输出 `.js` 会被 webpack 编译出无法解析的裸导入）；上传或校验失败必须阻止
  Vercel 上线；本地、Preview 和自托管不得上传。
  Cloudflare Token 只能授予 Account / Cloudflare Pages / Edit；EdgeOne 使用独立
  Makers API Token；二者都只能配置在 Web Production。业务 R2 Bucket 继续私有。
  构建若重新引入 `.mjs`，EdgeOne 的 `/_next/static/*.mjs` 必须返回 JavaScript
  MIME（`application/javascript; charset=utf-8`），不能是 `application/octet-stream`。

## UI 设计原则

- 参考 Notion、Claude、Reddit 等产品的信息密度与交互克制，但不复制品牌外观。
- 优先扁平信息层级、清晰间距和连续列表；减少嵌套卡片、色块、阴影和无意义圆角。
- 页面主任务必须显眼，低频操作放入三点菜单或次级区域。
- 按钮文字使用具体动作，例如“发布主题”“添加成员”“授权”，避免模糊的“确认”。
- 不写营销式、AI 式副标题。说明文字应直接描述用户能做什么。
- 空状态应紧凑且提供下一步，不得用大面积空白撑高页面。
- 异步数据页面必须先呈现稳定的页面框架和与最终内容近似的骨架，各独立数据区完成后分别替换；不得在首次请求完成前显示虚假的“空列表”或让多个辅助请求阻塞主内容。导航通知等次要数据应在主内容请求之后空闲加载。
- 危险操作与常规操作分开；不可执行的操作优先隐藏，只有需要解释时才禁用。
- 操作成功、警告和错误反馈统一使用页面顶部的全局悬浮通知，不在内容区插入反馈色条；表单字段自身的校验说明除外。
- 应用页面不显示独立的页面级大标题区，使用导航选中态、面板标题和内容自身标题提供上下文；详情页仍保留必要的返回或操作按钮，文档编辑器的可编辑文件名不受此规则影响。
- 应用页面（含 AI 对话页）的工作区组件统一缩小约 30%，不要在单个路由中重新放大按钮、表单或面板破坏整体密度。
- 移动端与桌面端使用同一套扁平视觉语言，不保留单独的渐变、大圆角外框或卡片化旧版界面。手机主导航使用“当前板块 + 下拉菜单”，不横向堆满所有入口；管理中心、文档/课件编排、练习创建与批改等密集工作流只支持电脑端，手机端提供明确说明而不是压缩复杂表格。浏览列表时缩短或隐藏次要文案，宽按钮优先改为图标操作。
- 移动端交互使用 `touch-action: manipulation` 消除误触双击放大，并将可编辑输入控件的计算字号保持在至少 16px，避免 iOS 聚焦时自动缩放页面；不得通过 `user-scalable=no` 或限制 `maximum-scale` 禁止用户主动双指缩放。
- 管理中心在电脑端使用独立的上下文侧栏，并按“人员与权限”“内容与资源”“系统与服务”组织子页面；子页面保留紧凑标题和用途说明，不再横向铺设全部管理入口。主导航只负责进入管理中心，上下文侧栏负责管理工具之间的关系。
- 高密度工作区只保留主操作使用深色实心按钮；次要操作使用浅底无边框样式，危险操作使用浅红底，表格和行内操作使用无底色文字或图标。布局优先连续平面、分隔线和留白，避免密集页面出现大量描边按钮与嵌套卡片。
- 配色统一使用 `redesign.css :root` 的语义变量，禁止再写近似色字面量：画布 `--bg`；填充层级 `--fill-subtle`（输入框/信息块）、`--fill-hover`（唯一列表 hover 灰）、`--fill-active`（选中/按下的中性底）；线条 `--line`（弱分隔）、`--line-strong`（结构线）；文字 `--text`/`--text-soft`/`--muted`；强调 `--accent`/`--accent-soft`/`--accent-dark`。黄色只用于选中状态、强调图标和必要提示。
- 页面级区块不再使用圆角矩形外框（无 1px 外框、无面板圆角）；结构层级由分隔线、双栏布局的竖线和留白提供。列表与工作面板使用透明背景，不使用白色大卡片。圆角只保留给控件和小元素：常规控件 10px、标签与小块 5-8px、弹窗 18px。
- 面板标题区是纯文字标题加底部通长分隔线；表格表头为透明底文字加分隔线；文档页等追求极简的页面可以省略标题区横线和分栏竖线，直接用留白分区。
- 状态筛选和紧凑视图切换统一使用论坛式分段控件：不显示整块底板或外框，选项平铺，仅为当前项提供浅色圆角底。
- 空表格行、空列表和空面板不是可交互列表项，不得响应列表 hover；空单元格背景始终与页面背景一致，禁止写死为白色。

## CSS 易错点

项目历史上 `apps/web/app/globals.css` 与 `apps/web/app/redesign.css` 存在多轮同名选择器。新增路由级改版时：

- 优先创建路由专属 CSS，并从该路由的 `layout.tsx` 引入。
- 明确重置继承的 `display`、`grid-template-columns`、`align-items`、`overflow`、`min-width`、高度和最后一项样式。
- 不要只覆盖颜色、边框和圆角；旧网格规则仍可能把输入框或列表项拉伸。
- 对 Grid/Flex 子项设置 `min-width: 0`，防止文本和表单横向撑破容器。
- 表单从双列改为侧栏单列时，必须显式写 `grid-template-columns: minmax(0, 1fr)`。
- 修改后检查 `:first-child`、`:last-child` 等旧选择器是否因 DOM 结构变化命中了错误元素。
- 透明圆角面板内如果首尾区块有独立背景，必须给首尾区块设置与外框匹配的内圆角，避免矩形背景覆盖外框转角；如果 hover 背景由嵌套列表行自身绘制，末行也必须匹配底部内圆角。不要通过裁切整个面板解决，否则三点菜单等浮层会被截断。
- 论坛首页的版块导航不是圆角面板，其底部分隔线必须保持笔直；路由专属样式应显式清除共享首尾子元素规则带来的圆角。

曾经出现并已修复的问题：

- 权限组成员区继承旧 `display: grid` 后自动拉伸，造成搜索框巨大和列表被截断。
- 成员头像插入第一列后，旧的 `> span:first-child` 命中头像，姓名和用户名失去纵向布局。
- 权限组右侧 280px 侧栏继承双列表单规则，导致“用途说明”被挤成竖排并横向溢出。
- 论坛全局多层覆盖产生巨大空白和圆角残留，后改为路由独立样式。

## 文档与权限

- 文件夹树接口返回 `fileCount` 和 `children`。文件夹允许连同全部子文件夹、文档及其业务数据递归删除；前端必须先展示影响范围，再要求输入文件夹名称进行第二次确认，后端仍需校验递归删除标记和名称。文件夹内的独立文件随文件夹一并永久删除（含存储对象），仍被课件引用的独立文件保留在文件库并解除归属；上传附件保留在文件库并解除与被删除目录、文档的归属关系。
- 单独删除文档同样是永久删除，不使用归档状态；删除时清理文档直接权限，文档拥有的业务数据由数据库级联删除，上传附件继续保留在文件库。
- 文件夹和文件的权限入口位于各自三点菜单；文件编辑页的权限入口也位于更多菜单。
- 文档页右侧内容区同时显示当前目录的子文件夹和文档，文件夹固定排在文档之前；支持按最近更新、名称和类型切换排序，点击文件夹名称或“打开”进入该目录。
- 文档页右侧列表只显示“文件名”和“最近更新”两个信息列及行尾操作，不单独显示类型、状态列；草稿文档在名称前显示紧凑“草稿”标签。右上角“新建”使用下拉选单，提供“新建文件夹”和“创建文档”，界面不将文档称为文件。
- 文档页左侧位置栏同时显示文件夹与直属文件，文件夹分支默认展开并可逐级折叠；置顶文件夹与文件直接作为右侧文档表格表头下方的前几行显示，不使用独立置顶面板。置顶及排序仅作用于当前目录的直接子项，进入其他目录时显示该目录自身的置顶结果；所有有权访问该资源的用户可见置顶项，仅系统管理员可置顶、取消置顶并调整当前目录内多个置顶项的顺序。置顶行仍保留完整三点操作菜单。
- 权限弹窗必须携带明确的 `folder` 或 `file` 目标，不能隐式使用当前选中文件夹管理文件权限。
- 权限级别为 `owner`、`editor`、`lecturer`、`viewer`、`no_access`。
- 权限按钮的显示与隐藏只是体验优化，服务层必须检查有效权限。
- 文档权限不使用权限组。管理员默认可编辑、普通成员默认可查看；系统管理员可在管理中心按单个成员设置 workspace 例外，也可在文件夹和文档三点菜单中设置逐级覆盖的成员例外。成员选择必须支持使用 tag 快速筛选。

## 已确认的产品决策

- 导航名称使用“论坛”，不使用“讨论”。
- 导航和页面名称使用“文档”，不使用“内容”或“课程内容”；“授课”统一称为“课件”，“素材库”统一称为“文件”。内部路由和代码标识暂不随界面名称调整。
- 创建在线测验不包含“选择测验范围”。
- 测验选项逐行添加，不让用户在大文本框中手动换行分隔。
- 课程演示根据一级、二级标题拆分页，支持方向键、空格、PageUp/PageDown、Home 和 End。
- “课堂”是课件、练习和课堂文件的唯一归属入口。系统管理员创建课堂并指派教师和学生，课堂教师可维护成员、课件、练习和课堂文件；学生只能访问自己所在课堂并作答其中练习。课件与练习必须且只能归属于一个课堂，系统管理员若未被指派为教师也不能编辑课堂内容。
- 主导航不单独展示“课件”“练习”或“文件”；课件与练习从课堂进入，文档附件文件页从文档页进入。普通文档附件只能在编辑文档时上传，失去全部文档及课件引用后自动删除；课堂文件使用独立存储记录，可上传 PDF、PPT 等供课堂成员下载，不受文档引用清理规则影响。
- 用户上传的独立文件、文档附件和课堂文件必须复用重命名时的资源名称规则：规范空白并拒绝空名、超长名称、控制/不可见字符及 `.`/`..`；不得静默替换非法字符。独立文件按所在文件夹、文档附件按所在文档、课堂文件按所在课堂拒绝同名上传；独立文件重命名也必须执行同目录判重。
- 独立文件、文档附件和课堂文件上传统一显示右下角任务浮窗与真实传输进度，支持一次多选、最多两个文件并发，每个任务可独立取消；等待、上传、失败与完成状态必须逐文件呈现，成功状态不得重复显示说明文字。浏览器取消或连接提前断开后，服务端必须补偿删除已写对象和预留数据库记录；若对象清理失败则保留可见记录供后续删除，不得制造不可见的孤立对象。超过 8MiB 的文件使用 8MiB multipart 分片，单个文件最多并发 4 个分片，会话状态保存在 Redis，服务端确认前必须列出并校验全部分片；小文件继续使用单请求。
- 独立文件、文档附件和课堂文件统一支持 PDF、Markdown 与 UTF-8 `.txt` 在线预览。PDF 预览在 direct 下载模式下由 API 校验读取权限后签发短期预签名 URL，浏览器直连对象存储流式按需加载（对象存储需放行 `Range` 请求头并暴露 `Content-Length`/`Accept-Ranges`/`Content-Range`；直连失败时前端自动回退 API 中转）；Markdown 与文本预览必须通过 API 重新校验当前读取权限并由服务器中转对象内容，不得使用公开或签名直链。PDF 预览上限 25MB，文本预览上限 2MB；Markdown 禁止原始 HTML 和远程图片，PDF 在浏览器内使用 PDF.js 渲染。其他格式继续下载。
- 文档页和课堂文件页点击文件名都先打开统一文件弹窗；支持的格式直接预览，不支持的格式在弹窗中显示文件信息和下载操作，不得让文件名点击直接触发下载。列表右侧可保留独立下载按钮。
- 用户上传的 favicon、头像、Banner 只有带内容版本号的 URL 才允许使用一年 `immutable` 缓存；文档附件和课堂文件的资源 ID 永久对应同一对象内容，可使用一年私有不可变缓存。公共设置允许浏览器保存但必须重新校验，附件下载、导出和签名跳转必须 `private, no-store`。favicon URL 可在浏览器本地保存经过路径校验的上一份配置，先恢复再后台校验，禁止缓存任意外部 URL。
- 登录页的演示账号由 `NEXT_PUBLIC_SHOW_DEMO_ACCOUNTS` 控制；该变量写入 Web 构建产物，生产容器修改后需要重新 build。
- 阿里云 OSS 的内网 Endpoint 只允许配合服务器中转下载；签名直出必须使用浏览器可访问的公网 Endpoint。管理界面应保持两者互斥，服务端必须拒绝冲突配置，并将历史遗留的冲突配置安全回退到服务器中转。OSS 默认域名会阻止浏览器内联预览签名 URL，因此图片、头像、Banner 和 favicon 等 `inline` 资源必须由 API 中转，仅明确下载的 `attachment` 可签名直出；PDF 预览在 direct 模式下的签名直出依赖 OSS 放行 `Range` 并暴露相关响应头，未配置时前端自动回退 API 中转。
- AI 设置支持保存多个命名模型配置并显式切换当前配置；配置使用常见 OpenAI 兼容服务商预设，只有“其他兼容服务”允许填写 API 地址。回答温度由服务端固定为 `0.2`，不作为管理员可调参数；当前配置不能直接删除，必须先切换。
- AI 回答后的参考资料只显示紧凑的文档链接，不提供展开段落或引用块详情的交互。
- AI 资料检索必须在权限过滤后覆盖全部可访问文档，不得以“最近更新”截断候选；按标题与内容片段分别评分，优先把实际命中的内容块送入上下文。连续对话检索和回答都应携带有限的近期对话历史，解决“它、这个、那组长”等追问指代；最终仍只保存并展示模型明确引用的资料。
- 文档支持 B 站视频区块。编辑器可粘贴官方 iframe 嵌入代码或视频链接，但服务端与渲染端只能提取并规范化 `player.bilibili.com` / `bilibili.com/video` 地址，禁止执行任意 HTML。官方播放器 iframe 需要声明 Chrome/Edge 新版 Local Network Access 权限策略，以兼容部分国内网络将播放器 CDN 解析到本地地址空间的情况；权限提示仅在 HTTPS 安全上下文中可用，HTTP 公网页面不得创建播放器 iframe，改为说明限制并提供“在 B 站打开”链接。
- 加载历史 AI 对话时必须重新校验引用文档是否存在且当前用户仍可访问；已删除、已归档或因权限变化不可访问的引用统一显示“文件不存在”，且不提供跳转链接。
- 论坛主题列表、主帖作者和各级回复优先显示用户上传的头像；没有头像时统一降级为姓名首字母。
- 用户徽章在个人主页、徽章管理预览等辨识场景显示图标与名称；论坛、课堂资源、练习、提交和管理表格等小字号高密度列表只显示保留原色的徽章图标，不显示文字、圆角矩形背景，仍通过无障碍名称和悬停说明提供徽章信息。
- 论坛支持匿名发帖和匿名回复，但数据库始终保留真实作者；API 必须对普通成员和管理员脱敏，只有最高管理员可以收到并查看匿名内容的真实作者身份。
- 论坛帖子标题、分类和主帖正文只有最高管理员可以修改；普通管理员仍可锁定、解锁和删除帖子，评论与嵌套回复发布后不提供编辑功能，评论作者和系统管理员仍可删除评论。
- 论坛帖子不使用归档状态；帖子作者或系统管理员删除帖子时永久删除整帖、全部回复和论坛专属图片。锁定仍用于暂停回复，但论坛列表不提供状态筛选栏目。
- 论坛主帖最多附带 9 张图片，评论和嵌套回复最多附带 3 张；浏览器上传前统一压缩为最长边不超过 1600px 的 WebP，服务端仍需根据首帖或回复分别校验真实文件头、尺寸、数量和容量。论坛图片作为帖子专属附件存储，不进入用户文件列表；匿名内容的图片响应不得暴露上传者或原始文件名。
- 论坛正文中的图片默认使用类似动态信息流的紧凑缩略图组，单图也不铺满正文；点击缩略图后在当前页面遮罩层查看大图，多图支持上一张、下一张、方向键和 Esc，不跳转新页面。
- 每个正常用户拥有独立个人主页，可自定义 Banner、头像、显示名和个人简介。业务界面展示作者时只显示可点击的显示名，不并排显示登录用户名；`@用户名` 只在个人主页和必要的账户管理界面显示。
- 论坛主帖、评论和嵌套回复均支持点赞与点踩；每位用户对同一内容最多保留一个方向，再次点击当前方向即撤销。
- 最高管理员可在系统设置上传、替换或重置全站网站图标；重置后清除网站 favicon 并刷新当前页，由浏览器显示自身的原生默认图标，不用网站绘制的近似图标冒充。自定义图标保存在对象存储中，通过公共只读接口提供，并用更新时间进行浏览器缓存失效。

## 数据与安全

- 系统角色为 `super_admin`、`admin`、`member`；文档权限只由系统角色决定：两类管理员均可创建和编辑，普通成员仅可查看已发布文档。
- 后端服务层是权限安全边界。
- 非公开 API 统一通过全局活动用户守卫校验账号状态和 `sessionVersion`；停用账号、修改密码或管理员重置关键账号属性后，旧会话必须立即失效。
- 登录失败次数使用 Redis 计数，`TRUST_PROXY_HOPS` 必须与 API 前实际可信代理层数一致，避免直接信任客户端伪造的转发头。
- 上传内容不得以内联方式提供 SVG；只有经过文件头识别的 PNG、JPEG、GIF 和 WebP 可以内联，其他类型强制下载并使用 `nosniff`。安全图片响应使用 `Cross-Origin-Resource-Policy: same-site`，以支持本地 Web 与 API 不同端口的预览；下载型附件继续使用 `same-origin`。
- AI 服务商 API Key 使用 `AI_ENCRYPTION_KEY` 进行 AES-GCM 加密后存入数据库；生产部署必须保留该密钥，否则已有配置无法解密。
- AI 请求由 Redis 实施单用户窗口限流和并发限制；流式请求在浏览器断开后必须取消上游请求。Nginx 必须关闭 API 代理缓冲并保留长于模型请求的读取超时。
- AI 调用按每日次数限额：`User.aiCallCount` 记录 `aiCallDateKey` 对应日期的用量，日期按 workspace 系统时区计算并跨日自动归零；`User.aiCallLimit` 为成员例外（null 跟随 `AiSettings.defaultCallLimit`）。`AiService.consumeCallQuota` 是所有 AI 调用的统一消耗点（条件递增，超限抛 429），任何新增 AI 调用入口必须先经过它；消耗发生在配置校验之后，AI 未启用或配置不完整时不扣次数。
- 图片和附件区块写入 `assetId` 时，服务端必须校验附件工作区和重新引用权限；归档文件中的旧区块不得继续授予附件下载权限。
- 所有 schema 变更都应提交 Prisma migration，不使用 `db push`；测试数据库需要重建时使用 `pnpm db:reset`。
- 消息列表的「删除」是软删除：仅写入 `NotificationRecipient.archivedAt` 隐藏消息，数据库行保留、无恢复入口；不要改写成物理删除。
- Release 部署必须自动生成 `SESSION_SECRET`、数据库密码和 MinIO 凭据。HTTP IP 模式使用 `SESSION_COOKIE_SECURE=false`；配置 HTTPS 后改为 `true`。

## 验证清单

提交前按风险选择并记录：

```bash
pnpm format:check
pnpm typecheck
pnpm test
pnpm build
# 或一次执行
pnpm validate
```

UI 修改额外确认：

- 页面没有横向滚动或截断。
- 输入框、下拉框和列表项没有被父级 Grid 拉伸。
- 弹窗在小屏内可滚动且操作按钮可达。
- 空、有少量数据、有大量数据三种状态均合理。
- 三点菜单在视口边缘不会溢出。

## 开发纪要

- 2026-07-11：完成全站 UI 重构的第一阶段，重点重做论坛、在线测验创建流程和内容页面。
- 2026-07-12：论坛改为路由独立样式；重做权限组管理与授课界面；修复权限组网格继承导致的截断和右栏错位；内容权限入口统一收进三点菜单；非空文件夹隐藏删除操作；权限弹窗改为紧凑授权面板。
- 2026-07-12：仓库首次发布至 `HFLive/liveboard`，默认分支为 `main`。
- 2026-07-12：系统角色拆分为 `super_admin`、`admin`、`member`。最高管理员拥有全站权限并独占系统与 AI 全局设置；管理员可管理文件、成员、权限组、容量和论坛，但不能管理其他管理员；普通用户的内容能力继续由资源权限组授予。系统必须保留至少一位正常状态的最高管理员。
- 2026-07-12：生产 Compose 增加一次性 `migrate` 服务并在 API 启动前执行 `prisma migrate deploy`；项目以完整初始 migration 为数据库基线，不再使用或兼容 `db push`。
- 2026-07-12：删除旧 `/app/files` 重定向和 v1 会话 Cookie 兼容；系统管理员不再自动加入或创建“管理员”权限组，系统角色与资源权限组保持独立。生产部署脚本会拒绝默认或过短密钥。
- 2026-07-12：页面主标题区只用于标识页面，不放业务操作按钮；创建、上传、导入、发布和编辑操作应放在对应列表、工具栏或内容工作区，返回操作使用标题上方的轻量返回链接。
- 2026-07-13：全站操作反馈统一为顶部悬浮通知；成功与错误不再作为内容区色条参与页面布局。
- 2026-07-13：新增独立“授课”板块；所有已登录用户可访问并创建课件，课件支持从内容节选段落、排序拼装、嵌套练习与全屏展示；文件编辑页移除直接授课入口。
- 2026-07-13：生产 Compose 的宿主机端口统一只绑定 `127.0.0.1`，公网访问必须经过反向代理；Web 镜像构建显式传入 `NEXT_PUBLIC_API_URL`，修改公开 API 地址后必须重新构建镜像。
- 2026-07-14：生产发布链路曾收口为单个 `liveboard-<version>-linux-amd64.tar.gz`，2026-07-26 已由 `.run` 自解压包取代。服务器拉源码构建、服务器下载多个 Release 文件和 v0.1.0 四文件兼容路径继续保持删除。包内脚本自动生成基础密钥、迁移、备份、等待服务健康并初始化唯一随机密码最高管理员。HTTP IP 与 HTTPS 通过 `SESSION_COOKIE_SECURE` 显式区分。
- 2026-07-14：发布前安全复盘收口附件重新引用授权、单次提交和最高管理员并发不变量、上传容量事务、AI 单用户限流与断开取消。API 健康检查改为实际探测 PostgreSQL、Redis、MinIO；部署备份默认保留最近 10 份；普通 PR 必须构建生产镜像并运行真实基础设施健康冒烟测试。
- 2026-07-14：统一非公开 API 的活动用户与会话版本校验，账号停用和密码变更可撤销旧会话；登录限流改为 Redis 共享计数。上传禁止 SVG 并按文件头限制可内联图片，AI API Key 改为 AES-GCM 加密存储；资源权限与练习列表改为批量查询，API/Web 容器使用非 root 用户运行。
- 2026-07-14：首次生产初始化的随机管理员凭据必须在部署总结末尾醒目显示，并保存到仅 root 可读的 `/opt/liveboard/initial-admin-credentials.txt`；升级沿用已有管理员，不能生成或覆盖密码。
- 2026-07-14：`apps/web/next-env.d.ts` 改为忽略的 Next.js 生成文件，Web typecheck 先执行 `next typegen`，避免开发与生产构建因 `.next-dev` / `.next` 路径不同反复污染 Git 工作区。
- 2026-07-14：AI 主页对话区改为连续阅读流：AI 回答不使用卡片，用户问题轻量右对齐，参考资料使用紧凑引用，输入框在底部自适应增高；该路由使用独立 `ai-workspace.css`，后续不要重新引入大面板嵌套和重复角色标签。
- 2026-07-14：应用侧栏账户区合并为单行：头像与姓名整体进入个人设置，右侧仅保留图标式退出按钮；不要重新增加独立“个人设置”和带文字的退出菜单项。
- 2026-07-14：内容系统支持将 UTF-8 `.md` 原子导入为普通文档，并将内容下载为 Markdown。Markdown 是交换格式而非正文存储源；标题支持一至六级，行内富文本只解释受限 Markdown，表格和数学公式使用独立区块。公式由 KaTeX 在 `trust: false` 下渲染，链接只允许 HTTP、HTTPS、mailto 和站内绝对路径；相对图片与不支持的语法采用明确可读降级，服务端不得抓取外链资源。
- 2026-07-14：内容编辑页在桌面采用整体左右分栏，左侧集中编辑区块格式与数据，右侧实时呈现整篇内容；窄屏回落为先编辑、后预览的上下布局，不再在每个区块内部重复显示预览。
- 2026-07-14：内容权限管理默认强调继承：文件夹和文件权限面板同时展示上级来源与当前例外，移除当前例外称为“恢复继承”；资源权限 `owner` 在界面统一称为“可管理”，避免与资源所有权转移混淆。日常授权优先放在文件夹层，文件级设置仅用于例外。
- 2026-07-14：管理中心新增“内容权限”，由系统管理员按权限组设置 workspace 内容默认权限；顶层文件夹从 workspace 继承，之后由子文件夹和文件逐级继承。workspace 授权只能由系统管理员查看和修改，普通成员即使获得内容“可管理”权限也不能修改全局默认值。
- 2026-07-15：界面产品名称统一调整为“文档”“课件”“文件”，分别替代原“内容”“授课”“素材库”；内部 `/content`、`/teaching`、`/library` 路由与代码标识保持不变。文档不再提供直接展示入口，只有已创建的课件可进入课堂展示。
- 2026-07-15：用户可在个人设置上传头像。前端必须在确认前提供裁剪编辑并输出 512×512 WebP；原图上传入口限制 2MB。后端仍需执行 2MB 上限与 PNG/JPEG/WebP 文件头校验，头像存储在对象存储中，不计入文件库附件容量。
- 2026-07-15：文档文件夹允许递归删除非空目录。删除前分两步确认并要求输入文件夹名称；数据库级联删除子目录、文档、区块、练习和提交记录，上传附件使用 `SET NULL` 保留在文件库。
- 2026-07-15：单独删除文档从归档改为永久删除，并同步清理文档直接权限；文档所关联的上传附件仍保留在文件库。
- 2026-07-15：文档页右侧列表改为同时展示当前目录的子文件夹和文档，并支持最近更新、名称和类型排序；文件夹树接口增加 `updatedAt` 供列表展示与排序。
- 2026-07-15：AI 页参考资料移除展开详情功能，仅保留可跳转到原文档的紧凑链接。
- 2026-07-15：历史 AI 引用在读取会话时批量复核文件存在性和当前用户权限；不可访问的旧引用统一降级为不可点击的“文件不存在”。
- 2026-07-15：移除应用各页面独立的主标题区（例如论坛页“论坛”大标题）；保留详情页必要操作以及文档编辑器的可编辑文件名。
- 2026-07-15：除 AI 对话页外，应用工作区的按钮、表单、面板、表格、菜单、弹窗和间距统一缩小约 30%，但不得使用 `zoom` 或整体 `transform: scale()`，页面内容仍须横向铺满可用区域。
- 2026-07-15：非 AI 高密度工作区统一轻量按钮与连续布局：主操作深色实心、次要操作浅底无边框、危险操作浅红底、行内操作无底色；减少面板描边、阴影与嵌套卡片。
- 2026-07-15：非 AI 页面保留浅灰画布，列表和工作区改用透明背景与 1px 外框区分区块；圆角统一为控件 10px、面板 14px、弹窗 18px。无语义差异的标题区、工具栏和表头统一浅暖灰，黄色只保留给选中与强调。
- 2026-07-15：论坛首页和详情页增加内容内边距并保持全宽；课件列表删除重复标题，将搜索和创建操作合并到同一工具栏。
- 2026-07-15：非 AI 页的状态筛选与紧凑视图切换统一为论坛样式；练习、提交记录和文件视图共享同一套分段控件视觉。
- 2026-07-16：文档左侧位置栏升级为可折叠的文件夹与文件混合树；右侧文档表格支持在每个目录内分别将直接子文件夹与文档共同置顶在表头下方，系统管理员可维护当前目录的多个置顶项及其顺序，普通用户只看到自身仍有权限访问的置顶资源；置顶行保留完整三点操作菜单。
- 2026-07-16：文档右侧列表移除类型和状态列，草稿状态改为名称前标签；右上角“新建”合并文件夹与文档入口，界面创建文案统一使用“文档”。
- 2026-07-17：论坛移除归档状态和状态筛选，删帖改为清理论坛图片并级联硬删除整帖；文档创建、Markdown 导入和新建文件夹入口按当前目录有效权限隐藏。练习创建页的可见范围改为按钮弹窗，创建完成后返回练习列表。课件展示固定使用 16:9 画布，窗口和全屏模式不再改变画布比例。
- 2026-07-17：论坛改为仅最高管理员可修改帖子；练习作答使用“提交”按钮加确认弹窗，不再插入检查提交步骤。练习详情移除管理按钮并增加轻量返回链接，提交管理统一称为“批改”，队列只保留“待批改”和“已批改”。练习开始、截止时间均为可选项。
- 2026-07-17：新增用户个人主页及 Banner、个人简介设置；侧栏和业务内容中的显示名可进入个人主页，登录用户名不再与显示名并排展示，仅在个人主页和账户管理场景保留。
- 2026-07-28：文档编辑器的行内链接入口改为可搜索的站内文档选择器，站内文档链接在当前页打开；桌面双栏编辑与预览按归一化阅读进度双向同步。段落格式工具栏固定在区块类型行内，以显隐而非插入布局的方式响应聚焦，避免正文跳动。
- 2026-07-28：文档的 `createdById` 与 `updatedById` 只保留为数据记录，不代表作者、所有者或通知订阅关系；消息中心不得因文档更新向创建者或最近更新者生成“你的文档已更新”通知。
- 2026-07-16：文档编辑器将图片与附件选择统一收进“插图”段落，移除顶部独立文件入口；跨文档内容块引用功能及其数据字段彻底删除，普通引用段落继续保留。
- 2026-07-16：课件与练习改为创建者维护逐用户可见范围，默认仅创建者本人；最高管理员始终可查看全部内容，但仍可像普通用户一样出现在选择列表中，其勾选状态不影响最高管理员的实际访问。课件快照中的图片和附件建立显式资源关系，可见课件会授予资源读取权限，且被课件使用的网盘资源必须拒绝删除并返回引用课件。
- 2026-07-17：课件编辑页的可见范围使用按钮打开弹窗设置，不在编辑区常驻展示用户列表。课件展示页禁止内容滚动：先根据实际画布逐级缩小文字和图片，超长列表、正文、代码和表格仍无法完整容纳时拆成连续页面，表格分页重复表头。
- 2026-07-17：文档页增加顶层“/”层级：右侧表格可直接列出并管理顶层文件夹（重命名、移动、权限、删除）；面包屑左侧返回按钮返回上一级目录，左侧“位置”标题直接回到顶层。当前目录记录在 localStorage，文档在新标签页打开后，“返回文档”回到最近打开的目录；没有记录或目录已删除时进入顶层。左侧位置树不再提供三点菜单，文件夹操作统一由右侧表格行尾菜单承担；置顶功能只在具体文件夹内可用。
- 2026-07-17：侧栏账户区移除独立的“个人设置”齿轮入口，恢复为头像与显示名进入个人主页、右侧仅保留图标式退出按钮；个人设置改由个人主页进入，不再在侧栏常驻。
- 2026-07-17：头像与 Banner 上传统一改为框选式裁切（`components/ImageCropDialog.tsx` + `imageCrop.ts`）：图片固定展示，中央选框可整体拖动、四角等比缩放，选框内容即最终图片；头像输出 512×512。2026-07-19 起 Banner 输出改为 1600×280，并在个人设置与个人主页统一使用约 40:7 的展示比例，避免裁剪与显示比例不一致。
- 2026-07-17：练习作答页改为路由独立样式 `exercises/[id]/exercise-runner.css`：题目列表为面板内连续行加单层分隔线，选项行为透明底 1px 外框、选中态用浅黄强调；右侧提交区信息改为标签值列表，提交记录为分隔线列表。旧的 `exercise-question-card`、`submission-result`、`choice-row` 等全局样式已删除，不要恢复嵌套白卡片。
- 2026-07-17：练习批改页改为路由独立样式 `exercises/[id]/submissions/review.css`：提交队列与批改详情改为透明面板 1px 外框，队列为透明连续行、选中浅黄；详情头部为浅暖灰标题区；逐题批改为连续行加分隔线，作答内容用浅暖灰块；得分与反馈为 110px + 自适应双列。旧的 `review-queue`/`answer-review` 白卡片样式已删除。
- 2026-07-18：管理中心全站统一为现行设计语言：`.admin-subnav` 改为论坛式分段控件（无底板、当前项浅灰圆角底）；总览 hub 与容量统计条改为透明面板内连续格；成员、论坛、权限组、文档权限、AI、系统设置各 tab 均收敛为透明面板 + 连续行 + 浅暖灰标题区。`admin-subnav`/`admin-hub-card`/`metric` 在 redesign.css 中的多轮同名覆盖已整体收口，不要再叠加新覆盖层；新增页面样式优先用路由专属 CSS。
- 2026-07-23：管理中心的信息架构改为桌面端上下文侧栏，取代顶部 9 项横向分段控件；入口按“人员与权限”“内容与资源”“系统与服务”分组，所有子页面恢复紧凑标题与用途说明。权限组详情改为成员优先、组信息置于其后的纵向结构，避免管理侧栏、权限组列表、成员和组信息四列同时争抢宽度。本条取代 2026-07-18 关于 `.admin-subnav` 使用横向分段控件的决定。
- 2026-07-18：全站移除页面级圆角矩形外框，改为扁平布局：双栏工作区用一道竖线分隔（`.workbench`/`.files-layout`/`.library-layout`/`.permission-groups-layout`/`.forum-admin-layout`/`.review-workspace` 的第二个子列），侧栏堆叠区块用横向分隔线，面板标题区改为纯文字标题加通长分隔线，表格表头去灰底。中央规则在 redesign.css 的 flat workspace 块，新增页面不要再给区块加外框和面板圆角。
- 2026-07-18：页面 gutter 抽为 `--page-gutter` 变量（当前 20px），出血规则统一引用；列表改为间距分隔 + 行 hover 圆角浅灰背景（类似 AI 历史记录），删除表内分行线；论坛版块选择收进帖子流工具栏的紧凑下拉；文档、帖子、个人主页改为新标签页打开并删除对应返回链接；用户可见的“文档类型”区分整体删除（创建表单、详情元信息、列表排序、AI 上下文），`exercise_set`/`asset` 类型仅作内部机制保留，API 创建文档默认 `doc`。
- 2026-07-18：文档页左侧位置树改为纯文件夹导航（参考 VS Code/Finder 侧栏），不再展示文档；文档统一在右侧表格呈现，消除树与表格重复展示同一批文档。新增或恢复树内文档展示需先评估是否破坏这一分工。
- 2026-07-18：全站 CSS 死代码清理：经全源码交叉比对删除约 70 个未使用类、220 个规则块（globals.css 与 redesign.css 净减约 1240 行），涉及旧首页 `home-shell`/`home-quick-card`、旧 AI 界面 `ai-hero-bar`/`ai-brand-panel`/`ai-chat-head`、旧论坛列表 `forum-thread-row`/`forum-compose-guide`、权限组旧 hero、`editor-action-bar`、`presenter-stage-toolbar` 等已下线界面。重新引入同名类前先确认不是恢复已删除的旧界面。活样式仍存在同一选择器在 globals.css 与 redesign.css 多轮定义的情况（约 280 组），属有意的覆盖分层，合并前需逐条比对级联。
- 2026-07-18：AI 对话页取消样式特例，与其他页面并列：redesign.css 和路由 CSS 中全部 `.workspace:not(.ai-workspace)`（131 处）改为 `.workspace`，AI 页同样继承全站密度、扁平工作区和组件规则；`ai-workspace.css` 继续以 `.ai-workspace` 前缀承载该路由专属布局（对话流、侧栏、输入区），平级时后加载的路由样式优先。2026-07-14 与 2026-07-15 纪要中"AI 页保持原有密度/非 AI 页"的表述由本条取代。
- 2026-07-19：减少全站重复的通长横线；面板标题与表头默认依靠留白和字重分层，不再统一绘制通长底线。论坛详情和成员管理等连续内容页只在确有结构歧义的位置保留局部分隔，避免每个工具栏、表头和列表行都形成一条横线。
- 2026-07-18：新增 AI 调用限额；2026-07-19 起改为按 workspace 系统时区每日重置。`AiSettings.defaultCallLimit` 为每日默认限额（默认 100，最高管理员在 AI 设置中调整），`User.aiCallLimit` 为成员例外（管理员在成员编辑弹窗设置，留空恢复默认）。侧栏账户行 hover 浮窗展示当日用量进度条（`GET /ai/usage`）。管理端用户列表/更新接口携带 `aiCallCount`/`aiCallLimit`，但 `toSummary` 等非管理序列化不暴露配额字段。
- 2026-07-18：测验与文档文件夹解耦：`ExerciseSet` 自带 `title` 和 `createdById`（migration 从原 exercise_set 文件回填），`fileId` 变为仅历史数据使用的可选关联。创建测验不再要求任何文件夹的授课权限，任何登录用户都可创建；查看答案、批改和管理权限统一为"创建者或系统管理员"，作答访问维持创建者 + 可见范围 + 最高管理员。列表项 `viaSuperAdmin` 标记仅最高管理员因特权可见的内容，前端标题以流动彩虹文字（`.rainbow-text`）标识。ExercisesService 不再依赖 PermissionsService。
- 2026-07-19：侧栏收窄至 116px：LB 标志与导航间距同步收紧，导航项内容居中；账户区只保留头像（新标签页进个人主页）和退出按钮，不再显示用户名；今日 AI 用量为常驻紧凑条（位于账户区分隔横线正上方，上行"今日 AI + 百分比"、下行进度条），hover 头像的浮窗只展示头像和显示名，退出按钮不提供任何 hover 浮层。练习列表的作者显示在"X 道题"之前；界面不再展示"X 个内容块"（课件列表、文档查看页元信息已移除）。
- 2026-07-19：无底色图标按钮（`.nav-button`、`.inline-icon-button`、`.icon-button`、`.table-action`、`.breadcrumb-back`、树折叠/置顶/三点菜单按钮、AI 页复制与历史三点按钮等）的 hover 不再添加背景，只做图标/文字颜色高亮；有底色或带边框的文字按钮、菜单项和行 hover 保持原有背景反馈。
- 2026-07-19：练习创建页改为路由独立样式 `exercises/new/quiz-builder.css`：左侧题目列表 + 题目编辑器、右侧基础设置 + 发布面板的双栏 workbench（复用全局竖线分栏与 panel-head 出血规则）；题型和判断题答案改用分段控件，题目行为间距分隔 + hover 圆角浅灰（编辑中为浅黄选中态），删除悬浮发布卡片，移动端回落单列（设置在前）并使用吸底创建条。redesign.css 中全部旧 `quiz-*` 规则已删除，不要恢复。
- 2026-07-19：帖子详情、练习批改和课件编辑页解除 1440px 居中上限并全宽左对齐。课件编辑页采用左侧内容编排、右侧文档段落与嵌套练习的双栏 workbench，窄屏回落单列；文档查看页的“未发布/已发布”改为标题前不换行的紧凑标签，顶部操作保持全宽，正文文字正常左对齐但整条 860px 可读栏在页面中居中；论坛发帖页同样使用 860px 居中编辑栏，论坛首页“发帖”入口保持深色实心主按钮样式。
- 2026-07-19：管理中心增加路由级 `admin.css` 统一全部面板：二级导航在窄屏保持单行横向滚动；总览改为“待处理事项 + 按角色显示的管理入口”；容量统计使用紧凑浅底摘要，论坛与设置表单限制可读宽度。系统设置窄屏先显示主表单、后显示状态摘要，避免只看到只读信息。后续管理页优先扩展该共享样式，不在 `redesign.css` 继续追加管理端覆盖。
- 2026-07-19：移动端应用壳改为“当前板块 + 下拉主菜单”，主栏不再横向排列全部入口，且菜单中不展示管理中心。`MobileRouteGuard` 统一阻止手机操作管理中心、文档/课件编排、练习创建与批改；阅读、播放、作答、论坛和个人主页继续适配小屏。移动端 AI 清除旧大圆角工作区与输入框阴影，保持与桌面端一致的扁平连续对话流；论坛正常帖子不显示“开放”，详情搜索与关注放在同一顶部操作栏。
- 2026-07-19：课件查看按设备采用两套呈现：桌面端继续使用固定 16:9 画布、拆页、翻页和全屏展示；820px 及以下改为连续文稿阅读，直接按课件原始内容顺序纵向呈现文档段落、图片、表格与课堂练习，不显示页码、页面导航、翻页、全屏或快捷键入口，也不锁定页面滚动。
- 2026-07-19：文件页详情在所有宽度下均为脱离文档流的悬浮层，桌面端从右侧覆盖、移动端从底部覆盖；选择文件不得切换列表网格列数或改变搜索、工具栏和文件列表的宽高。浮层打开时锁定背景滚动，可通过遮罩、关闭按钮或 Esc 关闭。
- 2026-07-19：论坛管理页不沿用贯穿视口的双栏结构竖线；版块列表和版块信息使用固定列宽与 32px 留白分区，布局高度跟随实际内容。2026-07-18 扁平工作区纪要中关于 `.forum-admin-layout` 竖线分栏的规则由本条取代。
- 2026-07-19：消息列表支持用户逐条叉掉消息；移除状态通过 `ActivityDismissal` 按用户和活动 ID 持久化，只影响当前用户。活动源后续产生新的更新时间和活动 ID 时仍可作为新消息出现。
- 2026-07-19：AI 流式请求确认已消耗额度后触发全局 `liveboard:ai-usage-consumed` 事件；应用侧栏先即时增加“今日 AI”用量，再请求 `/ai/usage` 校准。额度刷新请求使用 pending ref 串行补刷，避免与首次加载并发时丢失更新。
- 2026-07-19：AI 资料召回不再为所有提问强制填满来源：寒暄与普通对话跳过检索，文档必须有正相关词命中才进入上下文；生成提示要求用 `[资料N]` 标记实际使用的资料，服务端在保存与展示前移除标记，并只保留被标记的来源。
- 2026-07-23：文档新增安全的 B 站视频区块；系统设置新增全站网站图标上传、替换和重置，重置时清除图标并刷新当前页以恢复浏览器原生默认图标；论坛主帖与各级回复新增用户点赞、点踩和撤销；课件内嵌练习改为扁平连续题目、紧凑编号和统一选中态，提交区移除末题多余分隔线并统一使用深色主按钮。站内导航在点击时同步基础页面标题，详情数据加载后再覆盖具体标题，避免旧标题长时间残留。
- 2026-07-24：全站引入渐进式加载：`/app/loading.tsx` 负责路由切换骨架，列表与表格使用共享 `ProgressiveLoading` 占位；文档树与当前目录、管理总览各指标、AI 状态与历史记录等独立数据区分别完成并替换，禁止加载期间先显示空状态。`getMe` 对并发请求去重并短时复用，导航通知延后到浏览器空闲阶段加载，避免与页面主数据争抢。
- 2026-07-24：AI 资料召回移除“最近 80 份文档”截断，改为权限过滤后的全量候选、中文词组扩展、标题/短语/内容块相关度重排和命中片段级上下文；连续提问使用近期对话补全检索意图。文档、文件夹、课件、练习、帖子/版块、AI 配置及对话名称统一使用共享命名规则，拒绝空名、超长名、控制字符、零宽/双向控制字符和 `.`/`..`。
- 2026-07-25：新增课堂领域与独立课堂文件系统；课件、练习由逐用户可见范围迁移为单课堂归属，历史内容自动进入“历史课堂”并按原创建者/可见用户迁移教师与学生。主导航收口为“课堂”，文档页新增只读附件文件入口；普通附件仅允许从文档编辑器上传并在失去引用后自动清理。练习新增教师编辑入口，已有学生提交后锁定题目修改以保护历史答卷。
- 2026-07-26：彻底移除权限组；原权限组在迁移时转换为同名成员标签并保留成员关系。`super_admin` 与 `admin` 默认拥有文档创建、编辑和管理能力，`member` 默认只读；恢复不依赖权限组的成员级文档权限例外，workspace、文件夹和文档按层级继承，管理中心与资源权限弹窗均可按 tag 筛选成员。成员标签只用于组织与筛选，本身不授予权限。
- 2026-07-26：课堂成为登录后的默认首页，LB 标志直接进入课堂；课堂列表使用克制的圆角矩形块展示。课堂详情默认显示公告，课堂教师可发布、编辑和删除公告，学生只读。AI 不再占用主导航入口，改由文档页工具栏进入；管理中心删除总览页面，`/app/admin` 直接进入成员管理。
- 2026-07-26：管理员可在课堂详情编辑课堂名称和说明。AI 每日额度从全局应用侧栏移入 AI 历史栏左下角，仅在 AI 页面展示。文档左侧文件夹树的 hover 只作用于图标与名称按钮，不再铺满整行；每个文件夹增加与右侧列表共用操作逻辑的三点菜单，置顶仅在目标属于当前目录时提供。
- 2026-07-26：正式发布包改为可直接执行的 `.run` 自解压单文件；首次安装与升级分别使用 `install`/`upgrade`，固定状态收口到 `/opt/liveboard`，应用镜像按 Release 版本标记。新增 `liveboard` 管理命令统一状态、诊断、日志、启停、安全旧版本清理和保留数据卷的可恢复卸载；升级不再覆盖持久化 Nginx 配置。
- 2026-07-26：生产部署新增提供商无关的一键 HTTPS：Release 离线携带固定版本 ACME 客户端，最高管理员可在系统设置使用 HTTP-01 签发单域名证书；宿主机受限助手通过专用 Unix Socket 提供状态与启用操作，systemd timer 自动续期，切换失败会恢复原 Nginx、环境变量和安装状态。
- 2026-07-27：修复 HTTPS 助手在 `ProtectSystem=strict` 下无法完整运行 Ubuntu `nginx -t` 的问题；助手与续期 unit 精确开放 `/run/nginx.pid`、`/var/log/nginx` 和 `/var/lib/nginx` 并要求 Nginx 服务，不放宽整个 `/run` 或 `/var`。同步将 Nginx API 超时提高到 480 秒、API Socket 超时提高到 420 秒、前端安全地址跳转等待提高到 12 秒；升级器只迁移 LiveBoard 管理配置中旧的 150 秒超时，不覆盖其他自定义 Nginx 内容。
- 2026-07-27：一键 HTTPS 增加 TLS-ALPN-01 自动回退。公网 80 或 HTTP Host 被阻断时，助手临时将 Nginx reload 为 HTTP-only 配置以释放 443，完成验证后恢复 HTTPS；续期固定复用首次成功的验证方式。签发与续期失败均恢复原证书和 Nginx，避免证书文件已覆盖却只回滚配置。
- 2026-07-27：Release 固定携带的 lego v5.3.1 要求 `run` 子命令位于全部选项之前，续期同样使用 `run --renew-days`，不存在旧版 `renew --days` 命令。systemd timer 已负责随机错峰，因此续期传入 `--no-random-sleep`。Release 打包必须用真实 lego 二进制执行域名证书与 IP `shortlived` profile 的完整参数检查，不能只依赖忽略未知参数的 mock。
- 2026-07-27：HTTPS 管理支持停用并切回指定 HTTP 主机、自动续期开关和公网 IPv4 HTTPS。IP 证书使用 Let’s Encrypt `shortlived` profile，有效期约 6 天，进入 3 天续期窗口后更新；关闭自动续期只跳过 systemd 定时检查，手动续期仍可执行。停用 HTTPS 不删除已有证书，但会恢复 HTTP Nginx、`SESSION_COOKIE_SECURE=false` 和 HTTP `WEB_ORIGIN`。
- 2026-07-27：HTTP 访问地址改为独立于 HTTPS 状态的常驻配置，支持一个主地址和最多 7 个附加域名/IP。HTTPS 启用期间修改只保存降级预案，不触碰当前 Nginx、Cookie 或容器；HTTP 模式修改会先保留当前访问 Host、应用并本机验证全部 Host 路由，再更新 `WEB_ORIGIN` 和运行时。停用 HTTPS 默认使用已保存预案，不再要求临时输入地址。
- 2026-07-27：HTTP 与 HTTPS 会话改用不同 Cookie 名称：HTTPS 继续使用 `liveboard_session`，HTTP 使用 `liveboard_session_http`。这是因为浏览器会拒绝 HTTP 响应用同名非 Secure Cookie 覆盖降级前遗留的 Secure Cookie，表现为登录成功后立即回到登录页。HTTP 模式继续兼容升级前已有的 `liveboard_session`，HTTPS 模式不得接受 HTTP 专用 Cookie。
- 2026-07-27：课堂支持硬删除，仅 `super_admin` 与 `admin` 可操作。后端 `DELETE /classrooms/:id` 先删除 MinIO 中全部课堂文件对象（MinIO 删除失败则中止且不删数据库），再由数据库级联删除成员、公告、课堂文件记录、课件和练习（含题目与提交记录）；存储配额按行聚合实时释放。前端入口在课堂详情「编辑课堂」弹窗底部的危险区，复用文件夹递归删除的两步确认模式，第二步必须输入课堂名称。
- 2026-07-27：对象存储改为可切换后端，新增 `apps/api/src/modules/storage` 作为唯一存储入口（`StorageService` + `ObjectStorageBackend` 接口），MinIO 与阿里云 OSS（S3 兼容端点 `s3.oss-<region>.aliyuncs.com`，虚拟托管寻址）各一个实现；assets、classrooms、auth、settings、health 五个模块不再各自实例化 MinIO 客户端。配置存 `StorageSettings` 表（单行、按 workspace），OSS AccessKey Secret 复用 `AI_ENCRYPTION_KEY` 的 AES-GCM 加密；`backend`（minio/oss）与 `downloadMode`（proxy/direct）由最高管理员在管理中心「存储后端」页切换，保存 OSS 配置或启用 OSS 前服务端必须完成真实读写探测，失败拒绝保存。逐对象后端标记：`FileAsset`、`ClassroomFile`、`User.avatar/banner`、`Workspace.favicon` 均带 `storageBackend` 列，切换只影响新上传，历史对象按行内标记解析后端，不搬迁数据。下载直出通过 API 302 到预签名 URL（10 分钟，附件带 `response-content-disposition`），MinIO 后端和内网部署不支持签名，自动回退服务器中转；直出模式放弃 `nosniff` 与 CORP 头（签名 URL 无法覆盖），`Cache-Control` 也不复存在。健康检查依赖项 `minio` 更名为 `storage` 并探测当前激活后端。OSS 健康检查使用 `GetBucketLocation`（策略默认包含）而非 `bucketExists`：经实测，阿里云 S3 兼容网关把 HeadBucket 映射到独立的 `oss:HeadBucket` RAM 动作，`oss:GetBucketInfo`、`oss:GetBucketLocation`、`oss:ListObjects` 均不覆盖它，最小权限策略不应为健康检查额外引入该动作。同一网关对预签名 URL 不支持 `response-content-type` 覆盖（返回 400 `Can not override response header on content-type`），`response-content-disposition` 与 `response-cache-control` 可用，因此直出附件只靠 disposition 强制下载，不再覆盖 content-type。
- 2026-07-27：存储配额拆分为两套独立模型。`User.storageQuotaBytes` 改为只约束 FileAsset（文档附件+论坛图片），管理中心「存储容量」页改名「文档附件容量」；`Classroom.storageQuotaBytes`（默认 1GB）按 `classroomId` 聚合 ClassroomFile 约束课堂文件，系统管理员在「编辑课堂」弹窗调整（MB 粒度），新上限不得低于课堂当前已用，文件 tab 显示用量并在满额时禁用上传按钮。删除课堂、课堂文件、文件库附件、论坛图片、头像/Banner/favicon 旧版本均按对象 `storageBackend` 从对应后端（含 OSS）删除对象；删除文档按既有设计保留附件，失去引用后由惰性清理删除。
- 2026-07-27：配额模型改为「workspace 默认值 + 逐行覆盖（NULL 跟随默认）」：系统内置成员附件 128MB、课堂文件 512MB；`Workspace.memberAttachmentQuotaBytes`/`classroomStorageQuotaBytes` 为 workspace 级默认（管理中心「存储容量」页统一调节，`GET/PATCH /admin/users/storage/quota-defaults`），`User.storageQuotaBytes` 与 `Classroom.storageQuotaBytes` 为可空覆盖值，恢复默认即写 NULL。容量管理统一收口到 `/app/admin/storage`（成员表 + 课堂表 + 默认容量区），课堂编辑弹窗不再包含容量字段；容量页恢复「存储容量」名称。旧系统默认值在 migration 中重置为 NULL。本条取代同日早些时候的配额拆分条目。
- 2026-07-27：文档文件夹支持直接上传独立文件。`FileAsset.kind` 区分 `embedded`（文档/课件引用的附件）与 `standalone`（直接上传到文件夹的文件），`cleanupUnreferencedAssets` 只清理 embedded 且无引用的附件，standalone 永不进入惰性清理。standalone 必须有文件夹归属（权限 = 文件夹继承：editor/lecturer 可上传/改名/删除，viewer 可下载），类型白名单为 PDF、Office、txt/md/csv 与四种图片；`GET /files?folderId=` 返回 `{ files, standaloneAssets }`，文档页表格在文档之后展示独立文件（下载/重命名/删除）。独立文件不进文件库列表、不参与 AI 检索，容量计入成员文档附件配额。同时修复 multer/busboy 默认 latin1 解码导致的中文文件名丢失：三处 `FileInterceptor` 增加 `defParamCharset: "utf8"`（assets/upload、markdown 导入、课堂文件上传）。
- 2026-07-28：文档浏览页采用电子书式阅读排版：正文默认使用中文宋体衬线字体栈并限制为 760px 可读行宽，文档名称保持界面层级，正文一级标题不得再以同等字号和通长横线与文档名称竞争。阅读工具提供宋体、楷体、黑体和 16–24px 字号切换，并在浏览器本地持久化；移动端只保留阅读设置，不显示无法在手机操作的编辑入口。
- 2026-07-28：独立文件、文档附件和课堂文件上传改为 XHR 真实进度任务，并统一使用右下角可取消浮窗。上传控制器把浏览器断线转换为 AbortSignal；对象写完后才检测到中断时执行对象与数据库记录补偿清理。2026-08-02 起，大于 8MiB 的上传使用对象存储 multipart 分片：MinIO/OSS 由 API 接收并写入分片，OSS/R2 签名直入时浏览器按片 PUT，单个文件最多并发 4 片，确认阶段校验完整分片后完成对象；Redis 保存 multipart 会话并由取消/过期清理主动终止。
- 2026-07-29：文档附件、独立文件和课堂文件新增 PDF、Markdown 与 UTF-8 纯文本在线预览。预览接口复用既有访问控制并通过 API 中转 MinIO/OSS 内容，服务端校验扩展名、MIME、文件大小、PDF 文件头和 UTF-8 编码；前端按需加载 PDF.js，Markdown 禁止原始 HTML 与远程图片。
- 2026-08-02：Vercel/R2 下经过文件头校验的 PNG、JPEG、GIF、WebP 内联图片改为 API 鉴权后 302 到 120 秒预签名 GET，避免图片字节经过 Vercel；阿里云 OSS、自托管和 PDF/Markdown/TXT 预览仍保持 API 中转。Banner 等版本化 API URL 的签名重定向使用 `private, no-store`。favicon 与用户头像不使用 R2 签名重定向，始终由 API 中转；版本化 favicon 使用一年 `public, immutable`，版本化头像使用一年 `private, immutable` 缓存，未带版本号时重新校验。
- 2026-08-03：PDF 预览在 direct 下载模式下改为 API 鉴权后签发预签名 URL，浏览器直连对象存储流式加载（pdf.js `url` 模式 + `disableAutoFetch` 逐页按需拉取、首帧即渲染），并新增加载进度条与页码跳转。签名有效期 3600 秒以覆盖整个阅读会话；`application/octet-stream` 落库的 `.pdf` 同样可直传。直连失败（对象存储未放行 CORS/Range）或 `/preview-url` 探测失败时前端自动回退 API 中转。R2 CORS 需放行 `Range` 请求头并暴露 `Content-Length`/`Accept-Ranges`/`Content-Range`，见 docs/deploy-vercel-r2.md 7.3。本条取代同日预览接口相关描述。
- 2026-07-28：上传任务浮窗升级为多文件队列：文件选择器支持多选，全局按批次最多并发两个上传，每个文件独立显示等待、进度、失败、取消与完成状态；成功行只保留标题和文件名，不再在进度条下重复“上传完成”。文档编辑器首个文件替换当前插图块，后续文件按选择顺序插入连续内容块。
- 2026-07-28：全局反馈提示不再只用字符串作为 React 状态；重复触发相同文案时必须递增事件编号并重建通知节点，确保已经完成淡出动画的同文案提示可以再次出现。文档、课堂与文档编辑器已接入该通知状态。
- 2026-07-28：应用导航进一步收敛：桌面侧栏缩窄并使用弱中性选中底，移动端主菜单改为从当前板块旁展开的紧凑局部下拉；账户头像不再悬停弹出只重复姓名的浮层。论坛详情删除帖子内搜索，编辑、锁定和删除收进更多菜单，详情与回复统一使用 820px 居中阅读栏；论坛关注、`@用户名` 文本提及和“全部 / 未读”范围筛选整体删除，不再保存用户级帖子读取状态。个人主页采用通栏 Banner 与居中加宽的资料主体，头像不使用实体描边或环形光圈，只保留自然扩散的柔和阴影；论坛内容统一称为“帖子”。
- 2026-07-28：论坛列表的板块标签移到每行右上角，与标题分栏；共享用户徽章增加紧凑图标模式，用于论坛、课堂资源、课件/练习、提交队列和管理表格等小字号高密度场景，完整名称样式只留在个人主页及徽章辨识场景。
- 2026-07-29：移动端操作默认保持内容宽度，不再用全局规则把所有 `.button` 拉成 100% 宽；页面工具栏应让相关操作并排，明确图标可收成 44px 图标按钮，低频操作继续进入菜单。文档阅读的宋体与楷体改用应用自托管的 Noto Serif SC、LXGW WenKai Lite Web 字体，不能再只依赖手机系统字体名称。
- 2026-07-29：移动端解除文档编辑、课件新建/编辑、练习新建/编辑与提交批改的路由限制，`MobileRouteGuard` 只继续阻止管理中心。桌面双栏在 760px 以下不得简单纵向堆叠：文档使用“编辑/预览”、课件使用“大纲/添加内容”、练习使用“题目/设置”、批改使用“队列/详情”的单活动面板；触屏不依赖 hover 或拖拽完成关键操作，主要点击区域以 44px 为基准。本条取代 2026-07-14 的编辑/预览上下堆叠约定，以及 2026-07-19 对这些创作和批改路由的移动端禁用决定。
- 2026-07-29：消息系统改为持久化的 `Notification` 与 `NotificationRecipient`，站内消息是可靠主渠道。打开消息面板不再自动标为已读；用户可单条或全部标记已读、恢复未读、清除消息并按类别筛选。课堂公告、成员关系、练习发布、学生提交、批改完成和论坛回复必须在业务事务内创建消息；匿名论坛内容生成消息时不得保存或暴露真实作者身份。

- 2026-08-07：文档编辑页改为所见即所得单栏，取代 2026-07-14 的「左编辑/右预览」分栏与 2026-07-28 的双向滚动同步约定：整页随文档自然滚动，块默认按查看页同款宋体阅读排版渲染，点击块体（或 hover 铅笔、块菜单「编辑此块」）进入源码编辑态（markdown textarea + 类型选择 + 富文本工具栏），焦点移出块或按 Escape 退出并自动保存；预览面板整体移除。添加块表单没有提交按钮：输入后点击框外自动插入、空白点击框外取消，`table/divider/image` 等无文本输入框的类型选定即插入。大纲 sticky 常驻不离开视口（桌面顶 0 / 移动端 rail 下 58px），每项贴合文字宽度、与查看页移动端目录同款（否决过固定等宽方案）。大纲跳转必须精确着陆在页面顶端：手动计算 `getBoundingClientRect().top + scrollY - scrollMarginTop` 交给 `window.scrollTo`（原生 `scrollIntoView` 平滑滚动与 scroll-margin 组合在部分浏览器有着陆偏移 bug），`scroll-margin-top` 精确贴合固定区高度（移动端 104px = rail 58 + 大纲 46，桌面 45px = 大纲高度），余量过大时目标上方会露出上一个块的内容。文档容器与查看页底部留白均为 `min(80dvh, 720px)`，否则靠后的标题跳转受滚动边界限制无法到达页面顶端。
- 2026-08-09：Vercel 备份与回滚禁止通过 `fetch` 自调用同 Project 的 `/internal/cron/backup?jobId=` 续棒，平台递归保护会在有限层数后返回 508 并让任务永久停在「复制对象」。管理端创建任务后必须用 `@vercel/functions` 的 `waitUntil` 立即响应并在单次 Hobby Fluid Compute 生命周期内推进（预算 270 秒）；每日 Cron 同样在本次调用内尽量跑完存量任务，带 `jobId` 的入口只保留给外部 Cron/运维显式恢复。Vercel 任务列表进度必须回退读取 `BackupJob.progress`，不能只读不存在的本地状态文件。
